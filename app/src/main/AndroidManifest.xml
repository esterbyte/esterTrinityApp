<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ester.esterTrinityApp"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.ester.esterTrinityApp.TrinityApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_login"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_login_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.ester.esterTrinityApp.presentation.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.ester.esterTrinityApp.presentation.RegisterActivity">

        </activity>

        <activity android:name="com.ester.esterTrinityApp.presentation.MainHomeListActivity">

        </activity>

        <activity
            android:name="com.google.firebase.auth.internal.FederatedSignInActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleInstance"
            android:permission="com.google.firebase.auth.api.gms.permission.LAUNCH_FEDERATED_SIGN_IN"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            tools:replace="android:launchMode" />
    </application>

</manifest>